require 'spec_helper'

describe TechReader do
  it 'has a version number' do
    expect(TechReader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
